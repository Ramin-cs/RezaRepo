# سیستم ارسال ایمیل حرفه‌ای
# Professional Email Sender System Requirements

# کتابخانه‌های اصلی Python (معمولاً در پایتون موجودند)
# smtplib - برای ارسال ایمیل
# email - برای ساخت پیام‌های ایمیل
# ssl - برای اتصالات امن
# json - برای کار با فایل‌های JSON
# os, sys, time, random, re - کتابخانه‌های استاندارد
# datetime - برای کار با تاریخ و زمان
# pathlib - برای کار با مسیرها
# typing - برای type hints

# کتابخانه‌های اضافی (در صورت نیاز)
# jinja2>=3.0.0  # برای قالب‌سازی پیشرفته (اختیاری)
# beautifulsoup4>=4.9.0  # برای پردازش HTML (اختیاری)
# lxml>=4.6.0  # برای پردازش XML/HTML (اختیاری)
# dnspython>=2.1.0  # برای بررسی DNS و MX records (اختیاری)
# cryptography>=3.4.0  # برای رمزگذاری پیشرفته (اختیاری)

# توجه: این برنامه با کتابخانه‌های استاندارد پایتون کار می‌کند
# و نیازی به نصب پکیج اضافی ندارد