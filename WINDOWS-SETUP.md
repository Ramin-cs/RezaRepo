# 🪟 راهنمای نصب برای ویندوز

## ⚠️ مشکل Sharp در ویندوز

Sharp در ویندوز مشکل compatibility داره. ما این مشکل رو حل کردیم!

## 🚀 راه‌حل سریع:

### **روش 1: استفاده از فایل Batch (توصیه شده)**

1. **فایل `install-windows.bat` را اجرا کنید:**
   - روی فایل دابل کلیک کنید
   - یا در Command Prompt اجرا کنید:
   ```cmd
   install-windows.bat
   ```

### **روش 2: دستی**

#### **مرحله 1: Sharp را حذف کنید**
```cmd
npm uninstall sharp
```

#### **مرحله 2: پوشه‌ها را پاک کنید**
```cmd
rmdir /s node_modules
del package-lock.json
```

#### **مرحله 3: دوباره نصب کنید**
```cmd
npm install
```

#### **مرحله 4: برنامه را اجرا کنید**
```cmd
npm start
```

## 🎯 دسترسی به برنامه:

بعد از اجرای موفق:
- **آدرس**: `http://localhost:3000`
- **پورت**: 3000

## 🔧 اگر هنوز مشکل داری:

### **بررسی Node.js:**
```cmd
node --version
npm --version
```

### **اگر Node.js نصب نیست:**
1. به [nodejs.org](https://nodejs.org) بروید
2. **LTS version** را دانلود کنید
3. نصب کنید
4. کامپیوتر را restart کنید

### **اگر npm install کار نمی‌کنه:**
```cmd
npm cache clean --force
npm install --no-optional
```

## ✅ ویژگی‌های برنامه:

- ✅ **تحلیل واقعی تصاویر** با JIMP
- ✅ **تشخیص رنگ‌ها** و پالت رنگ
- ✅ **تشخیص layout** و ساختار
- ✅ **تولید کد ریسپانسیو**
- ✅ **پیش‌نمایش زنده**
- ✅ **دانلود فایل‌ها**

## 🎨 نحوه استفاده:

1. **تصویر خود را آپلود کنید**
2. **منتظر تحلیل بمانید**
3. **نتایج را مشاهده کنید**
4. **کد را دانلود کنید**

## 🆘 پشتیبانی:

اگر مشکل داری:
1. **Console errors** را چک کن
2. **Node.js version** را بررسی کن
3. **Firewall** را چک کن
4. **Port 3000** را آزاد کن

---

**🎉 موفق باشید!**